# Default connection (override with env or CLI in scripts).
# Source DB is used for generate-changelog and as reference for diff.
driver=com.mysql.cj.jdbc.Driver
username=${LIQUIBASE_USERNAME:-liquibase}
password=${LIQUIBASE_PASSWORD:-liquibase}

# Changelog; scripts override --url and --changelog-file as needed.
changelog-file=changelog/master.xml
